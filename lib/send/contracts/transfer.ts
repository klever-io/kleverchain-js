import { ISentContract } from "../../types";
import { ITransfer } from "../../types/contract";

const parseTransfer = (payload: ITransfer): ISentContract => {
  return null as any;
};

export default parseTransfer;
